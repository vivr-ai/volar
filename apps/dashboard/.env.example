# apps/dashboard environment variables
#
# Currently empty on purpose: this is still the placeholder app from issue
# 1.2 with no backend wiring. The first real variables land when Epic 2
# (Auth & Account Foundations) wires up the Supabase client — expected to
# be:
#   NEXT_PUBLIC_SUPABASE_URL=
#   NEXT_PUBLIC_SUPABASE_ANON_KEY=
#
# Set on Vercel under Project Settings > Environment Variables once added
# (separate values for Preview vs Production if they ever differ). Local
# dev: copy this file to .env.local and fill in values.
